create function dbo.whoAmI() returns varchar(5) as
begin

 declare @initials varchar(5)

 select top 1 @initials = ini from login where hostName = HOST_NAME() and hostId = HOST_ID() order by creationdatetime desc

 return @initials

end