create function dbo.whoAmI() returns varchar(max) as
begin

 declare @ret varchar(max)

 select top 1 @ret =
    ini
  + '|' + station
  + '|' + cast(creationDateTime as varchar)
 from login
 where hostName = HOST_NAME() and hostId = HOST_ID()
 order by creationdatetime desc

 return @ret

end