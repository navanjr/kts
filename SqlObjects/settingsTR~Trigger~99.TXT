create TRIGGER dbo.settingsTR ON dbo.settings AFTER INSERT, UPDATE AS
begin

 if not (update(modifiedBy)) and not (update(modifiedDateTime))
 begin
  update settings set modifiedBy = dbo.whoami() where id in (select id from inserted)
 end

end
