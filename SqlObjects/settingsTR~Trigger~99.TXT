create TRIGGER dbo.settingsTR ON dbo.settings AFTER INSERT, UPDATE AS
begin

-- if not (update(modifiedBy)) and not (update(modifiedDateTime))
-- begin
  update settings set modifiedBy = dbo.whoami(), modifiedDateTime=getdate() where id in (select id from inserted)
-- end

 insert settingsHistory
  select * from settings where id in (select id from inserted)

end
