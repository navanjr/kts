create function dbo.joinStr(
 @joinCharacter varchar(50),
 @data varchar(max)
) returns varchar(max) as
begin
 declare @retvar varchar(max)
 
 if isnull(@data,'') < ' 0'
  set @retvar = ''
 else
  set @retvar = isnull(@joinCharacter,',') + @data

 return @retvar

end