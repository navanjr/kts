create function dbo.taxYearPrintBRW( @taxYear varchar(50) ) returns @rt table(
 id int,
 ord varchar(50),
 code varchar(50),
 tally int
) as 
begin

 insert @rt 
 select 0,1,'Total Invoices',count(*) from invoices where taxyear = @taxyear

 return
end