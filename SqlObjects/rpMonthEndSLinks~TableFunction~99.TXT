create function dbo.rpmonthEndSLinks(@beginDate int, @endDate int) returns @rt table(
 journalSlink varchar(15),
 journalType varchar(50)
)
begin

 insert @rt select 'o' + cast(id as varchar), a18 from object where typ=4512 and key2 >= @beginDate and key2 <= @endDate

 return
end