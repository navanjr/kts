create function dbo.collectionsKeyCode(
 @dtype varchar(50),
 @parcel varchar(50),
 @item numeric(7,1)
) returns varchar(50) as
begin

 declare @retval varchar(50)

 if @dtype = 'RESALE'
  set @retval = @parcel
 else 
  set @retval = cast(@item as varchar)

 return @retval
end