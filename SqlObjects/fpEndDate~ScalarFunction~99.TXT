create function dbo.fpEndDate(
 @fpCode varchar(50)
) returns int as
begin
 set @fpCode = coalesce(@fpCode,dbo.fpCodeFromDate(null))
 return (select EndDate from dbo.fiscalCalendar where fpCode = @fpCode)
end