create function dbo.getInitialsFromFullName(
 @fullName varchar(max)
) returns varchar(50)
as
begin
 declare @returnString varchar(50) = ''
 select @returnString = @returnString + upper(left(ltrim(rtrim(data)),1)) from dbo.split(@fullName, ' ')
 return @returnString
end