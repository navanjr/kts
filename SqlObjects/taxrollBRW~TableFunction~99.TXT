create function dbo.taxrollBRW(
 @searchString nvarchar(50)=''
) returns @rettable table(
 name nvarchar(30),
 parcel nvarchar(24),
 taxYear numeric(5,0),
 item numeric(7,1),
 taxType nvarchar(1),
 amount money,
 due money
)
Begin
 insert @rettable
 select name,parcel,taxYear,item,typ,0,0
 from dbo.taxReference

 return

end

