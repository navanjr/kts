create function dbo.apiResultDecoder(
 @fieldName varchar(50),
 @json varchar(max)
) returns varchar(max) as
begin

 declare
  @retStr varchar(max)

 select @retStr = stringValue
 from dbo.parseJson(@json)
 where name = @fieldName

 return @retStr

end