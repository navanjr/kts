create function dbo.glJeDisplayAmount(@slink varchar(16)) returns money
AS
Begin
 declare @rv money

 select @rv=max(amount) from (
  select accounttype,sum(amount) as amount from gldetailreports where slink = @slink and amount > 0 group by accounttype) data
 return @rv

End