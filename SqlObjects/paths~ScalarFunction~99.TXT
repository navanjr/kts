create function dbo.paths() returns @rt table(
 name varchar(50),
 path varchar(max)
) as 
begin

 insert @rt select 'gitRepo', dbo.readstring('@gitpath=',b13) from object where typ = 0 and link1 = -1

 return 
end