CREATE FUNCTION dbo.removeExtraSpacesSF(@str AS VARCHAR(MAX))
RETURNS VARCHAR(MAX)
AS
BEGIN

set @str = rtrim(REPLACE(REPLACE(REPLACE(@str,' ','{{}}'),'}}{{',''),'{{}}',' '))
    RETURN @str
    
END