create proc dbo.createReasonsToCancel
as
begin
 set nocount on
 
 declare @reasons table(
  code varchar(50)
 )

 insert @reasons
  select 'Stop Payment'
--  union select ''


-- insert missing
 insert object (typ, key1)
 select 4773, code
 from @reasons
 where code not in (select key1 from object where typ = 4773)

 exec dbo.logit @@procid, 'Just inserted Reasons to Cancel into Object... @@rowCount', @@rowCount

end