create proc dbo.keyLogin (
 @initials varchar(5),
 @station varchar(50)
) as 
begin

 exec dbo.logit @@procid, 'Logging in @initials', @initials, '@station', @station

 insert login (ini,station) select @initials, @station
  
end