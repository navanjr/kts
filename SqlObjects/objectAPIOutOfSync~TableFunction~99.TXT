create function dbo.objectAPIOutOfSync(
 @typ int
) returns @rt table(id int) as
begin

 insert @rt 
 select id 
 from object
 where typ = @typ
  and cast(dbo.date1(LastEditDate)+' '+dbo.time2(LastEditTime) as datetime) > cast(k7 as datetime)
 order by id

 return
end
