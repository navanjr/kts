create function dbo.fpStartDate(
 @fpCode varchar(50)
) returns int as
begin
 set @fpCode = coalesce(@fpCode,dbo.fpCodeFromDate(null))
 return (select startDate from dbo.fiscalCalendar where fpCode = @fpCode)
end