create view dbo.fiscalCalendar as
 select
  id as fpId,
  key1 as fpCode,
  key2 as fpDesc,
  key3 as startDate,
  a1 as endDate,
  a17 as locked,
  a18 as fiscalYear
 from object
 where typ = 4700