create dbo.mortgageLinkGet (
 @taxYear varchar(4)
) as 
begin

 if not exists(select * from dbo.sysobjects where id = object_id(N'[dbo].[temp_mortgageLink]') and OBJECTPROPERTY(id, N'IsTable') = 1)
  create table(
   company varchar(100), 
   details varchar(max), 
   parcel varchar(100),
   record_id int,
   year varchar(50),
   site_id int
  )

  delete from dbo.temp_mortgageLink
  
  exec dbo.api

end