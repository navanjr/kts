create function dbo.receiptPayments(@recId int) returns @rt table(recTyp varchar(50),amount money)
as
begin


insert @rt
 
  select paycode+case when checkno>'  0' then '\'+checkno else '' end,amount from paid where slink = 'o'+cast(@recId as varchar)


 return
end