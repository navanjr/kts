create function dbo.getMiscDetail(@id int) returns varchar(500) as 
begin
 declare @retvar varchar(500)
 
 select @retvar = '@description=' + key1 + ';' 
  + '@source=' + key2 + ';'
  + '@fund=' + key3 + ';'
  + '@amount=' + a1 + ';'
 from object where typ=4503 and id = @id

 return @retvar
end

