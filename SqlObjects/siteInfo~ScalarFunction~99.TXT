create function dbo.siteInfo(
 @field varchar(50)
) returns varchar(50) as
begin

 declare @retval varchar(50)

 declare @wt table(
  id int identity(1,1),
  fieldName varchar(50),
	fieldValue varchar(50)
 )

 insert @wt select 'county', county from dbo.site()
 insert @wt select 'officialName', officialName from dbo.site()
 insert @wt select 'officialTitle', officialTitle from dbo.site()

end