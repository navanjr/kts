create function dbo.journalTypes() returns @rt table(journalType varchar(60)) as
begin
insert @rt
 select 'APPORTIONMENT'
  union
 select 'APPORTIONMENT SPECIAL'
  union
 select 'CORRECTION'
  union
 select 'MANUAL'
  union
 select 'Tax Refund'
return
end