create proc dbo.keyAcVars(
 @mode varchar(50) = 'LIST',
 @dump varchar(1000) = null output
) as 
begin

 if @mode = 'LIST'
 begin

  select @dump = e2 from object where typ = 0 and link1 = -1
 
 end
 else
 begin

  declare @wt 

 end


 exec dbo.logit @@procid, @dump

 return
end