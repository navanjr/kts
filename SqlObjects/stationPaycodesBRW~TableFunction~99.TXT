create function dbo.stationPaycodesBRW(
 @station varchar(50)
) returns @rt table(
 ord varchar(10),
 paycode varchar(50),
 accountCode varchar(50),
 accounttDesc varchar(50),
 balance money
) as
begin
 
 insert @rt (paycode) select key1 from object where typ = 4505

 return
end