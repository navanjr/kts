create function  dbo.docVariablesBlob(@docId varchar(50)) returns varchar(max) 
as 
begin
 declare @x varchar(max) = ''

 select @x = @x + '@' + dbo.splitf(settingName,'.',2) + '=' + ltrim(cast(settingValue as varchar)) + ';'from settings where parentId = 'o' + @docId

 return @x 
end