create function dbo.fpInfo(
 @clarionDate int
) returns @rt table(
  fpId int,
  fpCode varchar(50),
  fpDesc varchar(50),
  startDate varchar(50),
  endDate varchar(50),
  locked varchar(50),
  fiscalYear varchar(50)
) as
begin
 insert @rt select * from fiscalCalendar where cast(@clarionDate as varchar) between startDate and enddate
 return
end