create proc dbo.diagnosticsAutoHelper(
 @procId int,
 @blockAutoFix varchar(5)
) as
begin

 declare @cmd nvarchar(max)

 if @blockAutoFix = 'TRUE'
  return

 set @cmd = 'exec dbo.' + object_name(@procid) + ' @method=''FIX''';

 exec(@cmd)

 return
end