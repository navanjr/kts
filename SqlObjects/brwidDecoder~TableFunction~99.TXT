create function dbo.brwidDecoder(
 @brwid int
) returns @rt table(
 code int,
 id int
) as
begin

-- bail if not a valid brwid
 if @brwid > 0 
  return

 insert @rt
 select 
  abs(cast(left(cast(@brwid as varchar),2) as int)),
  substring(cast(@brwid as varchar),3,20)

 return
end
